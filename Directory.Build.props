<Project>
  <PropertyGroup>
    <RunnerVersion Condition="'$(RunnerVersion)' == ''">$([System.IO.File]::ReadAllText('$(MSBuildThisFileDirectory)VERSION').Trim())</RunnerVersion>
  </PropertyGroup>
</Project>
